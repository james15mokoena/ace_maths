import 'package:flutter/material.dart';

class TrigonometryPage extends StatelessWidget {
  const TrigonometryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("Trigonometry Content"));
  }
}
