import 'package:flutter/material.dart';

class ExponentsPage extends StatelessWidget {
  const ExponentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("Exponents Content"));
  }
}
